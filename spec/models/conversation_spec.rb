require File.dirname(__FILE__) + '/../spec_helper'

describe Conversation do
  before(:each) do
    @conversation = Conversation.new
  end

  it "should have many messages"
  
  it "should order messages by most recent first" 
  
  it "should ignore trashed messages" 
end
