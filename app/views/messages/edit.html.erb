<h1>Editing message</h1>

<%= error_messages_for :message %>

<% form_for(:message,
     :url => edit_person_message_url(current_person, @message)) do |f| %>
  <p>
    <b>Content</b><br />
  	<%= fckeditor_textarea("message", :content, :toolbarSet => 'InsoshiBase', 
			:width => '100%', :height => RICH_TEXT_HEIGHT) %>
  </p>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>
