<% form_for :post, :url => url, :html => { :method => method }  do |f| %>

  <div class="form_row">
    <%= f.text_area :body %>
  </div>

  <div class="form_row">
    <%= f.submit "Submit", :class => "button" %>
  </div>
<% end %>