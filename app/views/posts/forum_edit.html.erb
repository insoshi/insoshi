<h1>Edit post</h1>

<%= error_messages_for :post %>

<%= render :partial => 'forum_form',
           :locals => { :url => forum_topic_post_path(@forum, @topic, @post),
                        :method => :put } %>