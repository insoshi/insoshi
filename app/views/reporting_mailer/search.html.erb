<h3>Reqs</h3>

<ul>
  <% @reports.reqs.each do |req| %>
    <li><%= req.record %> : <%= req.person.try(:name) %></li>
  <% end %>
</ul>

<h3>Offers</h3>

<ul>
  <% @reports.offers.each do |offer| %>
    <li><%= offer.record %> : <%= offer.person.try(:name) %></li>
  <% end %>
</ul>

<h3>Memberships</h3>

<ul>
  <% @reports.memberships.each do |membership| %>
    <li><%= membership.record %> : <%= membership.person.try(:name) %>
  <% end %>
</ul>
