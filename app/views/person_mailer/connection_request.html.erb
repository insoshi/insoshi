This is an automated message from <%= app_name %>; please do not reply as there's no human at the other end of it.

You received a new connection request from <%= @connection.contact.name %> (<%= person_link 'profile', @connection.contact.name %>):

http://<%= @domain %><%= @url %>

<%= @preferences_note %>