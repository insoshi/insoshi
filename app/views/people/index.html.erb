<div class="column first span-14">

<% unless @people.empty? -%>
  <%= will_paginate %>
  <%= raster image_links(@people, :image => :thumbnail,
                                  :captions => @people.map(&:name)) %>
  <%= will_paginate %>
<% else %>
  <h2>No results to show.</h2>
<% end -%>

</div>

<%= render :partial => 'shared/minifeed' %>
