<%=
    collection_select( model_name,
                      'business_type_id',
                      BusinessType.all,
                      :id,
                      :name,
                      {},
                      {:multiple => false}
                      )
%>